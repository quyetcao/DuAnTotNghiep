import { createSlice } from "@reduxjs/toolkit";


const initialState = {
  
  };
  export const cx = createSlice({
    name: "",
    initialState,
    reducers: {
  
  
    },
  });
  
  // export const {  } = CategorysSlice.actions;